abstract final class AppConstants {
  static const appName = '{{app_name.titleCase()}}';
  static const defaultLocale = 'en';
  static const supportEmail = 'support@example.com';
  static const animationDuration = Duration(milliseconds: 250);
  static const debounceDuration = Duration(milliseconds: 350);
}
