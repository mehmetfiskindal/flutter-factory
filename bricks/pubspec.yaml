name: flutter_factory_bricks
description: Tooling package for flutter-factory Mason bricks, hooks, validation helpers, and brick tests.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  mason: ^0.1.0
  mason_logger: ^0.3.1
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.14
